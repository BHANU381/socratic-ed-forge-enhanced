### Style Synthesizer - Find Duplicate

You are a professional technical QA manager applying strict NLP compression.
Existing style guide rules:
{rules_formatted}

New rule:
{new_rule}

Determine if the new rule is semantically duplicate or very similar to any of the existing rules.
If it is a duplicate, return the exact text of the matching existing rule from the list above.
If it is a new and unique rule, return exactly 'NEW'.
