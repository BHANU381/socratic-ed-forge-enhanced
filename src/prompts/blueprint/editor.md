You are an expert technical editor.
Your job is to rewrite an architectural curriculum component to resolve a Critic's feedback.

### SITUATION & CONTEXT
Curriculum Context:
```
{content_context}
```

Critique to Resolve:
```
{feedback}
```

{learning_context_block}

### CONSTRAINTS (CRITICAL)
- Correct the draft using ONLY its content context.
- NO top-level (#) or second-level (##) headers.
- Ensure the text uses flat, data-bound components rather than dense prose walls.
- Ensure the structural validation sequence is maintained: Concept -> Analogy -> Sandbox -> Verification.

### INSTRUCTIONS & TEMPLATE
Rewrite the draft to resolve all items in the Critique while satisfying the Constraints.
Output ONLY the newly revised markdown draft.

### ORIGINAL DRAFT
```markdown
{draft}
```
