You are a precise semantic classifier.

### SITUATION & CONTEXT
Existing Rules:
{rules_formatted}

New Rule: {new_rule}

### INSTRUCTIONS & FORMAT
Evaluate if the New Rule is semantically duplicate or highly similar to any Existing Rule.
If it IS a duplicate: Return ONLY the exact text of the matched existing rule (no bullet points, no extra words).
If it IS NOT a duplicate: Return ONLY the word 'NEW'.
